
const IMAGE_BASE_URL = 'https://api.nomoreparties.co';
const shortMovieDuration = 40;

const moviesQuantityOnDesktop = 7;
const moviesQuantityOnMobile = 5; 
const regExpEmail = '^[a-zA-Z0-9]+@(?:[a-zA-Z0-9]+\\.)+[A-Za-z]+$';
const regExpName = '[a-zA-Z -]{2,30}';
export {
    regExpEmail,
    regExpName,
    shortMovieDuration,
    moviesQuantityOnDesktop,
    moviesQuantityOnMobile,
    IMAGE_BASE_URL
};